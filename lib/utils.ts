export const formatCompact=(value?:number)=>value===undefined||value===null||Number.isNaN(value)?'—':new Intl.NumberFormat('en',{notation:'compact',maximumFractionDigits:2}).format(value);export const formatUsd=(value?:number)=>value===undefined||value===null||Number.isNaN(value)?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:value<1?4:2}).format(value);export const formatPercent=(value?:number)=>value===undefined||value===null||Number.isNaN(value)?'0.00%':`${value>=0?'+':''}${value.toFixed(2)}%`;export const shortAddress=(value:string)=>`${value.slice(0,5)}...${value.slice(-5)}`;export const cn=(...parts:Array<string|false|null|undefined>)=>parts.filter(Boolean).join(' ');