import { NextResponse } from 'next/server';import { supabaseAdmin } from '@/lib/supabase';
export async function POST(request:Request){const form=await request.formData();const address=String(form.get('address')||'');if(supabaseAdmin&&address){const {data}=await supabaseAdmin.from('tokens').select('votes_24h,votes_all_time').eq('address',address).single();if(data){await supabaseAdmin.from('tokens').update({votes_24h:(data.votes_24h||0)+1,votes_all_time:(data.votes_all_time||0)+1}).eq('address',address);}}return NextResponse.redirect(new URL(`/token/${address}`,request.url));}