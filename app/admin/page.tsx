import { getTokens } from '@/lib/ton';import { formatCompact, shortAddress } from '@/lib/utils';
export default async function AdminPage(){const tokens=await getTokens();const promotedCount=tokens.filter((token)=>token.promoted).length;return <section className="container-main py-14"><div className="mb-8 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><h1 className="text-3xl font-bold">Admin dashboard</h1><p className="mt-2 text-slate-400">Moderate listings, toggle promoted placements, and wire in your own auth flow using Supabase.</p></div><div className="flex gap-3 text-sm"><div className="panel px-4 py-3">Listed: <span className="font-semibold text-white">{formatCompact(tokens.length)}</span></div><div className="panel px-4 py-3">Promoted: <span className="font-semibold text-white">{formatCompact(promotedCount)}</span></div></div></div><div className="card overflow-hidden"><table className="min-w-full text-sm"><thead className="bg-slate-950/30 text-left text-slate-400"><tr><th className="px-4 py-3">Token</th><th className="px-4 py-3">Address</th><th className="px-4 py-3">24h Votes</th><th className="px-4 py-3">All-time Votes</th><th className="px-4 py-3">Promoted</th><th className="px-4 py-3">Action</th></tr></thead><tbody>{tokens.map((token)=><tr key={token.id} className="border-t border-stroke/70"><td className="px-4 py-4 font-medium text-white">{token.name} <span className="text-slate-500">${token.symbol}</span></td><td className="px-4 py-4 text-slate-400">{shortAddress(token.address)}</td><td className="px-4 py-4">{token.votes_24h}</td><td className="px-4 py-4">{token.votes_all_time}</td><td className="px-4 py-4">{token.promoted?'Yes':'No'}</td><td className="px-4 py-4"><form action="/api/promote" method="post"><input type="hidden" name="address" value={token.address} /><button className="rounded-full border border-stroke px-3 py-2 text-xs hover:border-cyan-400/40">Toggle promote</button></form></td></tr>)}</tbody></table></div><div className="mt-6 card p-5"><h2 className="font-semibold text-white">Admin note</h2><p className="mt-2 text-sm text-slate-400">Add ADMIN_SECRET in your environment and protect the admin actions with middleware or a signed session. This starter keeps the UI ready while leaving auth decisions to you.</p></div></section>;}